name: Daily Digest

on:
  schedule:
    # UTC → Da Nang (UTC+7).
    # 0 0 * * * = 07:00 (brief + news), 0 11 * * * = 18:00 (brief only)
    - cron: "0 0 * * *"
    - cron: "0 11 * * *"
  workflow_dispatch:

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger digest on Railway
        env:
          CRON_SECRET: ${{ secrets.CRON_SECRET }}
          RAILWAY_PUBLIC_DOMAIN: ${{ secrets.RAILWAY_PUBLIC_DOMAIN }}
        run: |
          if [[ -z "$CRON_SECRET" || -z "$RAILWAY_PUBLIC_DOMAIN" ]]; then
            echo "CRON_SECRET and RAILWAY_PUBLIC_DOMAIN must be set in repository secrets."
            exit 1
          fi
          digest_url="https://${RAILWAY_PUBLIC_DOMAIN}/cron/digest"
          if [[ "${{ github.event.schedule }}" == "0 11 * * *" ]]; then
            digest_url="${digest_url}?news=0"
          fi
          curl -fsS --max-time 180 \
            -X POST \
            -H "Authorization: Bearer ${CRON_SECRET}" \
            "$digest_url"
